# Pipeline Health Report — PR #102

- Repo: byx-darwin/go-tools
- PR: #102 `feat(go-framework): add TLS-by-default + Functional Options to observability providers` (closes #96)
- Branch: `feat/96-observability-otel-tls-options` → base `main`
- Generated: 2026-09-03T09:02Z (via `gf` CLI, read-only)
- Context: PR just created; merge queued via `gf pr merge 102 --auto`

## 1. PR Pipeline Run

Run ID: [33736082606](https://github.com/byx-darwin/go-tools/actions/runs/33736082606)
Created: 2026-09-03T08:56:23Z

| Job | Status | Conclusion | Duration |
|---|---|---|---|
| go.mod hygiene | completed | success | 5s |
| go-common | completed | success | 89s |
| go-auth | completed | success | 49s |
| go-framework | completed | **failure** | 158s |
| go-middleware | completed | success | 279s |

**`go-framework` failed.** Root cause identified from job logs (`go mod tidy check` step, `go mod tidy -diff`):

```
diff current/go.mod tidy/go.mod
@@ -22,6 +22,7 @@
+ (blank line inserted in require block)
@@ -90,7 +91,6 @@
- (blank line removed in require block)
##[error]Process completed with exit code 1.
```

`go-framework/go.mod` is not tidy relative to the module's actual imports after this PR's changes (new `WithTLSConfig`/`tlsDialCredentials` code in `go-framework/hertz/observability` and `go-framework/kitex/observability`, plus the new `Insecure` config field). This is an actionable, PR-caused failure — not a flaky/infra issue. `gf pipeline report --branch feat/96-observability-otel-tls-options --days 14` correspondingly shows `totalRuns=1, successRate=0.0`, consistent with a real failed job (not just an in-progress snapshot artifact).

**Fix**: run `go mod tidy` inside `go-framework/` and commit the resulting `go.mod`/`go.sum` diff before the queued auto-merge proceeds — a required check is currently red.

## 2. Success-Rate Trend (dimension 1)

Baseline — `main`, last 14/30 days (`gf pipeline report --branch main --days 14|30`):

- 14d: 53 runs, 79.25% success, avg duration ~188s
- 30d: 55 runs, 76.36% success, avg duration ~187s

Both are below the skill's 80% health-threshold guidance, but the shortfall is fully explained by a historical, already-resolved cluster (see §3) — the last 26 of the most recent 30 `main` runs are all `success`, with only the current PR's `go-framework` job breaking that streak.

## 3. Failure Pattern Analysis (dimension 2)

Historical `main` failures in the last 30 days (`gf pipeline status --branch main`), all pre-dating this PR:

| Run ID | Created | Failing jobs | Cause |
|---|---|---|---|
| 33490082361 | 2026-09-01T09:01:51Z | go-middleware, go-framework | `go-auth@latest found (v0.1.0), but does not contain package .../go-auth/revocation` — go-auth module version lag |
| 33496010087 | 2026-09-01T10:10:09Z | go-middleware, go-framework | same as above |
| 33496823343 | 2026-09-01T10:19:29Z | go-middleware, go-framework | same as above |
| 33576217060 | 2026-09-02T00:39:21Z | govulncheck (go-framework), govulncheck (go-middleware) | known grpc vuln, reachable via `observability.NewProvider → metric.NewPeriodicReader`; fixed in `google.golang.org/grpc@v1.82.1` |

Both clusters are resolved: `go-auth/revocation` exists on disk today (merged via #72), and `go-framework/go.mod` already pins `google.golang.org/grpc v1.82.1` (the fixed version). No repeat of either signature in 26+ subsequent `main` runs.

**New, distinct failure on PR #102**: `go-framework` job fails at the `go mod tidy check` step (see §1) — a different signature from either historical cluster, and specific to this PR's own diff (new TLS/options code without a `go mod tidy` pass). Not flaky — deterministic given the current `go.mod` state; will fail on every rerun until fixed.

## 4. Duration (dimension 3)

- `main` avg run duration: ~187s (3.1 min) over 14/30 days — no reported outliers.
- Per-job breakdown from a recent green `main` run (33714346343): `go.mod hygiene` 3s, `go-auth` 51s, `go-common` 89s, `go-framework` 160s, `go-middleware` 227s (longest — driven by redis/kafka/db/es/clickhouse integration tests). Consistent with PR #102's own per-job durations above; no new bottleneck introduced by this PR.

## Findings Summary

| # | Severity | Finding |
|---|---|---|
| 1 | 🔴 blocking | PR #102's `go-framework` CI job fails at `go mod tidy check` — `go-framework/go.mod` is out of date after this PR's changes. This will block the queued auto-merge (`gf pr merge 102 --auto`) until fixed. Fix: run `go mod tidy` in `go-framework/` and push the resulting diff. |
| 2 | 🟡 informational | `main`'s 14/30-day success rate (76–79%) sits below the 80% guidance threshold, but is fully attributable to two already-resolved historical clusters (dependency-version lag on `go-auth/revocation`, and a `govulncheck` grpc CVE now patched at v1.82.1) — not an active/ongoing regression. |

## Conclusion

One actionable, blocking finding: PR #102's `go-framework` job fails `go mod tidy check` due to this PR's own changes — the queued auto-merge will not proceed until `go-framework/go.mod` is tidied and re-pushed. All other jobs on this PR (`go.mod hygiene`, `go-auth`, `go-common`, `go-middleware`) are green, and duration profile matches `main`'s baseline with no new bottleneck. This is a read-only analysis; no pipelines were triggered, retried, or cancelled, and no CI config was modified.
