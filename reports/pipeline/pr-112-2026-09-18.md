# Pipeline Health Report — PR #112

- Repo: byx-darwin/go-tools
- PR: #112 `chore: sync go-auth/go-middleware/go-framework sibling require to latest go-common` (closes #111)
- Branch: `feat/111-sync-sibling-go-common` → base `main`
- Generated: 2026-09-18T06:40Z (via `gf` CLI, read-only)
- Context: PR open, not yet merged. Diff is `go.mod`/`go.sum` only (bumps `go-common` v0.1.0 → v0.3.1 in `go-auth`, `go-middleware`, `go-framework`), no code changes (`gf pr diff 112`).

## 1. PR Pipeline Runs

Two workflow runs triggered on this branch/PR head:

### Run A — CI test suite (`go.mod hygiene` + per-module build/test), Run ID [35315027246](https://github.com/byx-darwin/go-tools/actions/runs/35315027246)

Created: 2026-09-18T06:28:27Z — **all jobs succeeded**

| Job | Status | Conclusion | Duration |
|---|---|---|---|
| go.mod hygiene | completed | success | 6s |
| go-auth | completed | success | 55s |
| go-common | completed | success | 100s |
| go-framework | completed | success | 184s |
| go-middleware | completed | success | 287s |

### Run B — CodeQL / govulncheck, Run ID [35315027245](https://github.com/byx-darwin/go-tools/actions/runs/35315027245)

Created: 2026-09-18T06:28:27Z — **`govulncheck (go-framework)` failed**

| Job | Status | Conclusion | Duration |
|---|---|---|---|
| CodeQL | completed | success | 123s |
| govulncheck (go-common) | completed | success | 9s |
| govulncheck (go-auth) | completed | success | 28s |
| govulncheck (go-middleware) | completed | success | 55s |
| govulncheck (go-framework) | completed | **failure** | 44s |

Root cause from job logs (`govulncheck ./...` step in `go-framework`):

```
Vulnerability #1: GO-2026-6348
    Heap Memory Exhaustion (OOM) via HTTP/2 DATA Frame Fragmentation in
    google.golang.org/grpc
  Module: google.golang.org/grpc
    Found in: google.golang.org/grpc@v1.82.1
    Fixed in: google.golang.org/grpc@v1.83.1
  Example traces:
    #1: hertz/observability/provider.go:133:40: observability.NewProvider calls metric.NewPeriodicReader, ... mem.MaterializeToBuffer
    #5: builder.go:436:17: oops.OopsErrorBuilder.WithContext calls obj.Prog.String, ... mem.writer.Write
    ... (15 call traces total, all through metric.NewPeriodicReader / oops error builder paths)
Your code is affected by 1 vulnerability from 1 module.
##[error]Process completed with exit code 3.
```

**Not caused by PR #112.** The PR's diff (`gf pr diff 112`) touches only `go-common` version pins in `go.mod`/`go.sum` across the three sibling modules — `google.golang.org/grpc` is untouched (still pinned at v1.82.1 before and after this PR). `GO-2026-6348` is a newly-published advisory (post-2026-09-03, when PR #102's report last checked govulncheck and found it clean at grpc v1.82.1) that now flags the already-pinned grpc version, reached transitively via `go-framework/hertz/observability` (OTel metric exporter) and `go-common`'s `oops` error builder. This will fail on **every** rerun/PR touching `go-framework` until grpc is bumped to v1.83.1+, regardless of PR content — it is a repo-wide, pre-existing dependency exposure, not something introduced by this sync PR.

## 2. Success-Rate Trend (dimension 1)

- `main` (push-triggered CI workflow only), `--days 14`: 4 runs, 100% success, avg duration ~207s.
- `main`, `--days 7`: 3 runs, 100% success, avg duration ~219s.
- Repo-wide (all workflows/branches incl. PR-triggered CodeQL/govulncheck), `--days 30`: 61 runs, 81.97% success, avg duration ~193s, `topFailures`: `go-framework`, `go-middleware`, `govulncheck (go-middleware)`, `govulncheck (go-framework)`.

Interpretation: the push-to-`main` CI workflow (the 5-job build/test suite) has been fully green for the last 2 weeks — no regression from recent merges. The lower repo-wide 30-day figure is driven by the separate CodeQL/govulncheck workflow, whose `topFailures` list matches the exact failure seen on this PR (`govulncheck (go-framework)`), confirming this is a recurring, systemic signature rather than a one-off. 82% is still above the skill's 80% health-threshold guidance, so tier is 🟢, unchanged from the 🟡-adjacent readings in the prior two reports (PR #98: 77.55%, PR #102: 76–79%) — no 3-consecutive-same-tier escalation applies.

## 3. Failure Pattern Analysis (dimension 2)

| Report | Failing signature | Cause | Status |
|---|---|---|---|
| PR #98 (2026-09-02) | `go-framework`/`go-middleware` → `govulncheck` | resolved grpc CVE cluster (09-01→09-02) | resolved, no recurrence at the time |
| PR #102 (2026-09-03) | `go-framework` `go mod tidy check` | PR-introduced go.mod drift | fixed by that PR |
| PR #102 (2026-09-03) | historical `govulncheck` grpc CVE | already patched to v1.82.1 by that point | resolved at the time |
| **PR #112 (2026-09-18, this report)** | `govulncheck (go-framework)` | **new** CVE `GO-2026-6348` against grpc v1.82.1 (the version that fixed the *previous* CVE) | **active, unresolved** |

This is a **new, distinct** advisory from the one closed out in the PR #102 report — same job/module keeps recurring as a failure point because `google.golang.org/grpc` in `go-framework` has not been re-bumped since v1.82.1 was pinned, and the vulnerability database has moved forward again. Deterministic (will fail on every govulncheck run against `go-framework` until the dependency is bumped) — not flaky.

The `go-framework`/`go-middleware` CI-suite jobs themselves (Run A) show no flakiness across this PR's two consecutive runs or the last 14 days of `main`.

## 4. Duration (dimension 3)

- PR #112 Run A per-job: `go.mod hygiene` 6s, `go-auth` 55s, `go-common` 100s, `go-framework` 184s, `go-middleware` 287s (longest, as usual — redis/kafka/db/es/clickhouse integration tests) — consistent with the `main` 14/30-day baseline (~187–219s avg) and PR #102's per-job baseline. No new bottleneck introduced by this go.mod-only change.
- CodeQL/govulncheck workflow (Run B) is fast (9–123s per job) and not a duration concern.

## Findings Summary

| # | Severity | Finding |
|---|---|---|
| 1 | 🟡 actionable, not PR-caused | `govulncheck (go-framework)` fails on PR #112 due to a newly published advisory `GO-2026-6348` against `google.golang.org/grpc@v1.82.1` (fixed in v1.83.1), reached via `go-framework/hertz/observability/provider.go:133` (`metric.NewPeriodicReader`) and `go-common`'s `oops.OopsErrorBuilder.WithContext`. Confirmed unrelated to this PR's diff (go-common version bump only, grpc untouched). Will recur on every future `go-framework` PR until grpc is bumped to v1.83.1+. This is the same job signature (`govulncheck (go-framework)`) that appears in the repo-wide 30-day `topFailures` list — a recurring, systemic gap, not a one-off flake. |
| 2 | 🟢 informational | PR #112's actual CI test suite (`go.mod hygiene`, `go-auth`, `go-common`, `go-framework`, `go-middleware`) is 100% green across two consecutive runs on this branch, and `main`'s push-triggered workflow has been 100% green for 14/7 days — no regression from this change. |
| 3 | 🟢 informational | Duration profile matches historical baseline; no new bottleneck. |

## Conclusion

PR #112's own build/test pipeline is fully healthy and the change (a pure `go.mod`/`go.sum` version bump) introduces no regression. One actionable-but-unrelated finding: the separate govulncheck workflow fails on `go-framework` due to a new grpc CVE (`GO-2026-6348`, fixed in v1.83.1) — this is a pre-existing, repo-wide dependency exposure that will block every future `go-framework`-touching PR's govulncheck check (if it is a required check) until a follow-up dependency bump lands; it does not block PR #112's own correctness. Recommend a follow-up chore PR to bump `google.golang.org/grpc` to ≥v1.83.1 in `go-framework` (and verify `go-middleware`, which shares the dependency per `topFailures`). This is a read-only analysis; no pipelines were triggered, retried, or cancelled, and no CI config was modified.
